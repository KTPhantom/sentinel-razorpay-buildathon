# Transaction Anomaly Explainer — Dependencies
# Pinned from `pip freeze` on Python 3.13 (Windows), verified working environment.
# Per 02_architecture_spec.md §5: pin versions for reproducibility.

# Core ML & data
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
scipy==1.18.0
joblib==1.5.3

# LLM API
anthropic==0.125.0

# Config / env
python-dotenv==1.2.3

# Dashboard (v1.5 — optional)
streamlit==1.62.0
altair==6.2.2
pyarrow==25.0.1

# Plotting (EDA script)
matplotlib==3.11.1
